import { Template } from "../../core/types.ts";

const Header: Template = {
  render: `
    <header class="header">
        <i class="ti ti-file"></i>
        <h1>Notes</h1>
    </header>
`,
};

export default Header;
